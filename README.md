# ArrayOperations


Приложение для выполнения операций над массивами с строгим соблюдением принципов контрактного программирования и визуальной индикацией условий выполнения.

## 🚀 О проекте

Array Operations Application - это десктопное приложение, разработанное для демонстрации практического применения методологии Design by Contract в сочетании с архитектурой MVVM. Приложение позволяет выполнять различные операции над числовыми массивами с визуализацией предусловий и постусловий операций.

### Основные возможности

- **Три базовые операции**: сортировка, поиск максимума, вычисление суммы
- **Визуальная индикация контрактов**:实时 отображение состояния предусловий и постусловий
- **Контрактное программирование**: строгая проверка Pre/Post условий
- **Подробная документация**: встроенная справка и описание контрактов
- **Обработка ошибок**: информативные сообщения при нарушении условий

## 🏗️ Архитектура

Приложение построено по архитектуре **MVVM (Model-View-ViewModel)** с четким разделением ответственности:

### Backend слой (Models)
- **`ArrayOperation`** - абстрактный базовый класс операций
- **`SortOperation`**, **`MaxOperation`**, **`SumOperation`** - конкретные реализации операций
- **`OperationContract`** - модель данных для описания контрактов
- **`Guard`** - механизм проверки предусловий

### Business Logic слой (ViewModels)
- **`MainViewModel`** - управление операциями и навигацией
- **`OperationViewModel`** - логика выполнения операций и валидации

### Frontend слой (Views)
- **`MainWindow`** - главное окно приложения
- **`ContractWindow`** - окно отображения контрактов операций
- **`HelpWindow`** - окно справки

### Ключевые принципы
- **Контрактное программирование**: каждая операция имеет четкие Pre/Post условия
- **Реактивность**: автоматическое обновление UI при изменении данных
- **Иммутабельность**: операции не изменяют исходные данные
- **Расширяемость**: легко добавить новые операции через наследование

## 🛠️ Технологический стек

- **Платформа**: .NET 8.0
- **UI Framework**: WPF (Windows Presentation Foundation)
- **Архитектура**: MVVM
- **Тестирование**: xUnit
- **Язык программирования**: C# 11.0

## 📋 Системные требования

### Минимальные требования
- **Операционная система**: Windows 10 (версия 1809) или новее
- **.NET Runtime**: .NET 8.0 Desktop Runtime
- **Память**: 512 MB RAM
- **Процессор**: 1 GHz или быстрее
- **Разрешение экрана**: 1024x768

### Рекомендуемые требования
- **Операционная система**: Windows 11
- **.NET Runtime**: .NET 8.0 Desktop Runtime
- **Память**: 1 GB RAM
- **Процессор**: 2 GHz многоядерный процессор
- **Разрешение экрана**: 1920x1080

## 🚀 Запуск приложения

### Способ 1: Готовый релиз
1. Скачайте последний релиз из раздела [Releases](https://github.com/yourusername/array-operations/releases)
2. Установите .NET 8.0 Desktop Runtime если не установлен
3. Запустите `ArrayOperations.exe`

### Способ 2: Сборка из исходного кода
```bash
# Клонирование репозитория
git clone https://github.com/yourusername/array-operations.git
cd array-operations

# Сборка решения
dotnet build

# Запуск приложения
dotnet run --project ArrayOperations
```

### Способ 3: Открытие в Visual Studio
1. Откройте `ArrayOperations.sln` в Visual Studio 2022
2. Восстановите NuGet пакеты
3. Нажмите F5 для запуска

## 🧪 Тестирование

Проект включает комплексные юнит-тесты:

```bash
# Запуск тестов
dotnet test

# Запуск с покрытием кода
dotnet test --collect:"XPlat Code Coverage"
```

### Основные тестовые сценарии
- **Тестирование операций**: корректность сортировки, поиска максимума, вычисления суммы
- **Проверка контрактов**: валидация Pre/Post условий
- **Граничные случаи**: пустые массивы, отрицательные числа, единичные элементы
- **Обработка ошибок**: корректность исключений при нарушении условий

## 📖 Использование

1. **Выберите операцию** из списка слева
2. **Введите массив** чисел через запятую (пример: `1, 2, 3, 4`)
3. **Следите за индикаторами**:
   - 🟢 Зеленый - условие выполнено
   - 🔴 Красный - условие не выполнено
4. **Нажмите "Выполнить"** для запуска операции
5. **Просмотрите результат** в нижней части окна

### Доступные операции

| Операция | Предусловие | Постусловие |
|----------|-------------|-------------|
| Сортировка | Массив не пуст | Массив упорядочен, мультимножество сохранено |
| Максимум | Массив не пуст | Возвращен максимальный элемент |
| Сумма | Массив не null | Возвращена сумма элементов |

## 🔧 Разработка

### Добавление новой операции
1. Создайте класс, наследуемый от `ArrayOperation`
2. Реализуйте методы `CheckPreconditions`, `Execute`, `GetContract`
3. Добавьте операцию в коллекцию в `MainViewModel`

```csharp
public class NewOperation : ArrayOperation
{
    public NewOperation() : base("Название", "Описание") { }
    
    public override bool CheckPreconditions(int[] array, params object[] parameters)
    {
        // Проверка предусловий
    }
    
    public override (int[] result, bool success) Execute(int[] array, params object[] parameters)
    {
        // Логика операции
    }
    
    public override OperationContract GetContract()
    {
        // Описание контракта
    }
}
```

